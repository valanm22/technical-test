services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - 8000:8000
    volumes:
      - type: bind
        source: ./backend/app/alembic/versions
        target: /app/app/alembic/versions
    develop:
      watch:
        - action: sync
          path: ./backend
          target: /app
          ignore:
            - .venv/
        - action: rebuild
          path: ./backend/pyproject.toml
    depends_on:
      db:
        condition: service_healthy
        restart: true
    env_file:
      - ./.env
  db:
    image: postgres:16-alpine
    container_name: db
    ports:
      - 5432:5432
    volumes:
      - postgres_db:/var/lib/postgresql/
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      retries: 5
      start_period: 30s
      timeout: 10s

volumes:
  postgres_db:
